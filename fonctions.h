/**
 * @brief transform a character 
 * 
 * @param letter 
 * @param code_bin 
 */
void transfo_Ascii(char letter, char* code_bin);
/**
 * @brief transform into a text file that has  binary txt
 * 
 * @param filename 
 */
void transfo_bin(char *filename); 
/**
 * @brief count the number of character in a text file
 * 
 * @param filename 
 * @return int 
 */
int count_char(char *filename); 
